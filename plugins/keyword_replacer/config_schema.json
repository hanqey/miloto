[
  {
    "key": "rules",
    "label": "替换规则",
    "type": "list",
    "hint": "每条规则把「原词」替换成「替换为」。转发前对所有文本段生效。",
    "item_schema": [
      {"key": "from", "label": "原词", "type": "string", "ph": "如：微信"},
      {"key": "to", "label": "替换为", "type": "string", "ph": "如：WeChat"}
    ]
  }
]
